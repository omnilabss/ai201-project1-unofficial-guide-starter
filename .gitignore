# API keys — never commit these
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# ChromaDB local storage
chroma_db/
chroma/

# Raw downloaded HTML — provenance only, regenerable via scripts/collect_documents.py.
# The cleaned documents/*.txt + documents/sources.json are the committed corpus.
documents/raw/

# macOS
.DS_Store
