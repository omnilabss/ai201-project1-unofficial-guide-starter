sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Document ingestion (Milestone 1 collector: scripts/collect_documents.py).
# Downloads source HTML; text extraction uses only the Python stdlib (html.parser),
# so no trafilatura/bs4 needed.
requests>=2.32.0

# Query interface (Milestone 5): Gradio web UI (app.py) + a CLI (ask.py)
gradio>=6.9.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
